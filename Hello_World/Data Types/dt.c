#include <stdio.h>
#include <stdlib.h>

int main()
{

    // Interger datatype

    int a = 3;

    // Float datatype (this print the numbers after the decimal point)

    float b = 3.142;

    // Char datatype

    char c = 'J';
    printf("\n %d \n %f \n %c", a, b, c);
    return 0;
}